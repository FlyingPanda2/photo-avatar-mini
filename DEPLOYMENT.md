# 📸 Telegram Mini App: Фото-аватар с AI генерацией

Telegram Mini App для создания красивых аватаров с AI генерацией и локальной обработкой фото.

## 🚀 Быстрый старт для Telegram

### Шаг 1: Развернуть бэкенд на Render.com (ВАЖНО!)

1. **Перейти на Render**: https://render.com
2. **Создать новый Web Service**:
   - Нажмите "New +" → "Web Service"
   - Подключите этот GitHub репозиторий
   - **Name**: `photo-avatar-api` (или любое имя)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python backend.py`
   
3. **Добавить переменную окружения**:
   - Нажмите "Environment"
   - Добавьте:
     - **Key**: `REPLICATE_API_TOKEN`
     - **Value**: (ваш токен от https://replicate.com/account/api-tokens)
   
4. **Нажмите "Create Web Service"**
5. **Скопируйте URL** вашего сервиса (будет что-то типа `https://photo-avatar-api.onrender.com`)

### Шаг 2: Обновить index.html

В `index.html` замените URL бэкенда:
```javascript
const BACKEND_URL = 'https://photo-avatar-api.onrender.com'; // Ваш URL с Render
```

### Шаг 3: Обновить Telegram бот

Используйте `https://t.me/YOUR_BOT_NAME/photo-avatar` в качестве Mini App URL (где `YOUR_BOT_NAME` — имя вашего бота).

Если Mini App уже создан, обновите его в BotFather:
```
/mybots → Выберите бот → Web App → Отредактируйте URL
```

## 📋 Структура проекта

```
photo-avatar-mini/
├── index.html          # Telegram Mini App (на GitHub Pages)
├── backend.py          # Flask сервер (на Render.com)
├── requirements.txt    # Зависимости Python
├── Procfile           # Конфиг для облачного хостинга
└── README.md          # Этот файл
```

## 🎨 Функции Mini App

### 1. Загрузка фото или AI генерация
- 📤 Загрузить фото с устройства
- 🤖 Сгенерировать через AI нейросеть
- 💬 Опциональное описание для AI

### 2. Три стиля обработки
- **Аниме** (Anime illustration, manga style)
- **Винтаж** (Vintage polaroid, 1970s film grain)
- **Пиксель-арт** (Pixel art, 8-16 bit retro style)

### 3. Скачивание результата
- ⬇️ Скачать как PNG
- 🔄 Переключение стилей (←→)
- 📤 Выбрать другое фото

## 🔑 Получить API ключ Replicate

1. Перейти на https://replicate.com
2. Нажать "Sign in" (создать аккаунт, если нет)
3. Перейти на https://replicate.com/account/api-tokens
4. Скопировать токен
5. Добавить в Render.com в Environment переменной `REPLICATE_API_TOKEN`

**Бесплатный лимит**: ~30 генераций в месяц

## 🛠️ Локальное развитие

### Установка
```bash
pip install -r requirements.txt
```

### Запуск
```bash
# Создайте .env файл
echo REPLICATE_API_TOKEN=your_token_here > .env

# Запустите бэкенд
python backend.py
```

### Тестирование
- Откройте `index.html` в браузере локально
- Приложение автоматически выберет `http://localhost:5000`

## 🌐 Архитектура

```
Telegram Bot
    ↓
GitHub Pages (index.html) ← Render.com API (backend.py)
    ↓
User Device (Canvas processing)
```

- **Frontend** (index.html): на GitHub Pages, обрабатывает локально пиксель-арт и винтаж
- **Backend** (backend.py): на Render.com, генерирует аниме через Replicate AI
- **Кэширование**: на сервере, чтобы экономить бесплатный лимит

## 📝 API endpoints

### POST `/generate`
Генерирует изображение через AI
```json
{
  "style": "anime",
  "prompt": "красивая девушка с голубыми глазами"
}
```

**Ответ:**
```json
{
  "url": "https://...",
  "cached": false,
  "style": "anime"
}
```

### GET `/health`
Проверка статуса
```json
{
  "status": "ok",
  "api_configured": true,
  "cache_size": 5
}
```

## ⚠️ Важные замечания

1. **GitHub Pages**: Убедитесь, что репозиторий публичный
2. **CORS**: Бэкенд настроен принимать запросы с GitHub Pages
3. **Кэш**: Сгенерированные изображения кэшируются на сервере
4. **Лимит**: Replicate бесплатный (30 поколений/месяц) - используйте оптимально

## 🔗 Полезные ссылки

- **Telegram BotFather**: https://t.me/botfather
- **Replicate API**: https://replicate.com
- **Render.com**: https://render.com
- **Telegram WebApp API**: https://core.telegram.org/bots/webapps

## 🐛 Troubleshooting

### Ошибка: "Cannot reach backend"
- Проверьте, что backend URL в `index.html` верный
- Проверьте статус сервера: `https://your-backend-url/health`

### Ошибка: "REPLICATE_API_TOKEN не установлен"
- Добавьте переменную окружения в Render.com
- Перезапустите сервис

### Генерация идет очень долго
- Первый запрос может занять 60+ секунд
- Используйте кэш (повторите тот же стиль + промпт)

### GitHub Pages не обновляется
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Проверьте, что `index.html` в ветке `main`

## 📄 Лицензия

MIT - используйте свободно!

---

**Создано для Telegram Mini App 2026** 📱✨
