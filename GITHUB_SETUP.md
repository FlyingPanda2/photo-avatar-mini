# 🚀 GitHub Mini App: Полная настройка

## ✅ Что нужно сделать один раз

### 1️⃣ Получить API ключ Replicate

1. Перейти на https://replicate.com
2. Нажать "Sign in" (создать аккаунт бесплатно)
3. Перейти на https://replicate.com/account/api-tokens
4. **Скопировать токен** (сохраните его!)

### 2️⃣ Добавить секрет в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. **Name**: `REPLICATE_API_TOKEN`
5. **Value**: (вставьте ваш токен)
6. Нажмите "Add secret"

### 3️⃣ Обновить URL в index.html

Откройте `index.html` и найдите строку:
```javascript
const REPO = 'YOUR_USERNAME/photo-avatar-mini'; // ← Замените!
```

**Замените** на ваш реальный URL репозитория:
```javascript
const REPO = 'honor/photo-avatar-mini'; // Пример
```

### 4️⃣ Включить GitHub Pages

1. Откройте репозиторий → **Settings** → **Pages**
2. **Source**: `Deploy from a branch`
3. **Branch**: `gh-pages` / `root` (если используете главную ветку)
4. Нажмите **Save**

Ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/photo-avatar-mini/
```

### 5️⃣ Обновить Telegram бот

В **BotFather** установите Mini App URL:
```
https://YOUR_USERNAME.github.io/photo-avatar-mini/index.html
```

---

## 🎯 Как это работает

```
Пользователь нажимает "Генерировать"
        ↓
index.html отправляет repository_dispatch событие
        ↓
GitHub Actions запускает workflow (.github/workflows/generate.yml)
        ↓
Workflow вызывает Replicate API
        ↓
Изображение скачивается и коммитится в gh-pages
        ↓
index.html загружает изображение и показывает пользователю
```

---

## ⚠️ Важные моменты

### Времени выполнения
- Генерация: 30-60 сек
- Проверка: до 60 сек
- **Итого**: может занять 2-3 минуты

### Бесплатный лимит Replicate
- ~30 генераций в месяц (бесплатно)
- После лимита нужен платный план

### Личный токен GitHub (опционально)
Если хотите ускорить проверку статуса, добавьте личный токен:

1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (Classic)
3. Выберите `repo` и `workflow` скопы
4. Скопируйте токен
5. В index.html замените:
```javascript
const GITHUB_TOKEN = 'ghp_YOUR_TOKEN_HERE';
```

---

## 🐛 Troubleshooting

### Ошибка: "Ошибка при запуске генерации"
- ❌ Проверьте, что `REPO` в index.html установлен правильно
- ❌ Убедитесь, что репозиторий публичный

### Ошибка: "Убедитесь, что добавлен REPLICATE_API_TOKEN"
- ❌ Проверьте, что токен добавлен в Settings → Secrets
- ❌ Убедитесь, что название secret правильное: `REPLICATE_API_TOKEN`

### Генерация идет очень долго
- ⏳ Это нормально (30-60 сек за генерацию)
- 💾 Результат сохраняется в папку `/generated` для повторного использования

### GitHub Pages не обновляется
- 🔄 Очистите кэш браузера (Ctrl+Shift+Delete)
- 📝 Убедитесь, что файлы в ветке `gh-pages` или главной ветке

---

## 📁 Структура репозитория

```
photo-avatar-mini/
├── index.html                      # Telegram Mini App
├── .github/
│   └── workflows/
│       └── generate.yml            # GitHub Action для генерации
├── generated/                      # Папка с сгенерированными изображениями
│   └── *.jpg                       # Результаты
└── README.md
```

---

## 🔐 Безопасность

✅ API ключ хранится в **GitHub Secrets** (не видна в коде)
✅ Генерация происходит на сервере GitHub Actions (не в браузере)
✅ Результаты сохраняются в репозитории

---

## 📝 Пример для вашего случая

Если ваш GitHub: `honor/photo-avatar-mini`

1. Откройте Settings → Secrets → New secret
2. Name: `REPLICATE_API_TOKEN` 
3. Value: (ваш токен)

4. В index.html измените:
```javascript
const REPO = 'honor/photo-avatar-mini';
```

5. Развернитесь на GitHub Pages (Settings → Pages → gh-pages)

6. Сайт: https://honor.github.io/photo-avatar-mini/

Готово! 🎉

---

## 💬 Первый запуск

После настройки:
1. Откройте приложение
2. Нажмите "🤖 Генерировать через AI"
3. Введите описание (опционально)
4. Выберите стиль
5. Ждите 1-3 минуты
6. Изображение появится автоматически!

**Наслаждайтесь!** 🚀✨
